// File Manager types and interfaces

// Re-export FileMetadata from main types
export { FileMetadata } from '../../types';

// Re-export FileManager interface
export { FileManager } from './interfaces';